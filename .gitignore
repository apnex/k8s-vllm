# Venv lives outside the repo by convention, but if anyone ever creates one
# inside, do not track it.
venv/
.venv/
vllm-venv/

# Model weights are large and not part of the install. Downloaded via
# Hugging Face cache or vLLM's automatic fetch.
models/
*.safetensors
*.bin
*.pt
*.gguf

# Python cruft
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Test transient output (canonical results live in archive/ once captured)
/root/aorus-vllm-tty-test/
